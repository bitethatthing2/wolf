// This is a type declaration file for next.config.js
// It exists solely to satisfy TypeScript's requirement

import { NextConfig } from 'next';

// Declare the module for next.config.js
declare const config: NextConfig;
export default config;
